class Procedure {
  constructor(id, categoryIndex, code, fee, name) {
    this.id = id
    this.categoryIndex = categoryIndex
    this.code = code
    this.fee = fee
    this.name = name
  }
}

export default Procedure
