class Note {
  constructor(id, tag, data, pinned, createdAt, createdBy) {
    this.id = id
    this.tag = tag
    this.data = data
    this.pinned = pinned
    this.createdAt = createdAt
    this.createdBy = createdBy
  }
}

export default Note
